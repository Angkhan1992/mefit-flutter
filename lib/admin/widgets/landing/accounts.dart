import 'package:flutter/material.dart';

class LandingAccountsView extends StatelessWidget {
  const LandingAccountsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
