import 'package:flutter/material.dart';

class TermsAgreeView extends StatelessWidget {
  const TermsAgreeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
